server {
    listen 80;
    # Replace 'your_domain.com' with your actual domain
    server_name your_domain.com;

    location / {
        include proxy_params;
        # Ensure the path matches the socket path in the systemd service file
        proxy_pass http://unix:/home/user/smoking_tracker/smoking_tracker.sock;
    }
}
